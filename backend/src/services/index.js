/**
 * services/index.js — Business logic layer
 *
 * 모든 비즈니스 규칙은 Service에만 작성합니다.
 * Controller → Service → Repository
 */
module.exports = {
  // bookingService: require('./booking.service'),
  // authService: require('./auth.service'),
};
