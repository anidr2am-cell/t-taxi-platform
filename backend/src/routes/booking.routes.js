const express = require('express');
const router = express.Router();

// TODO: vehicle/recommend, pricing/calculate, CRUD bookings
// Mount sub-paths matching OpenAPI /bookings/*

module.exports = router;
