/**
 * controllers/index.js — Controller registry (optional)
 *
 * 구현 시 feature별 controller export
 */
module.exports = {
  healthController: require('./health.controller'),
  // authController: require('./auth.controller'),
};
