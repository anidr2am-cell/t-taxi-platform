const BOOKING_STATUS = require('./reservationStatus');

const ADMIN_BOOKING_VIEWS = {
  NEEDS_ACTION: 'needs_action',
  ISSUES: 'issues',
  TODAY: 'today',
  UPCOMING: 'upcoming',
  IN_PROGRESS: 'in_progress',
  SETTLEMENT: 'settlement',
  COMPLETED: 'completed',
  CANCELLED: 'cancelled',
  ALL: 'all',
};

const OPERATIONS_SEVERITY = {
  URGENT: 'URGENT',
  SOON: 'SOON',
  REVIEW: 'REVIEW',
};

const OPERATIONS_THRESHOLDS = {
  UNASSIGNED_URGENT_BEFORE_MS: 30 * 60 * 1000,
  DRIVER_ARRIVED_PICKUP_DELAY_MS: 30 * 60 * 1000,
  PICKED_UP_LONG_TRIP_MS: 6 * 60 * 60 * 1000,
  STATUS_STALE_MS: 2 * 60 * 60 * 1000,
};

const DEFAULT_HISTORY_DAYS = 30;

const TERMINAL_BOOKING_STATUSES = [
  BOOKING_STATUS.COMPLETED,
  BOOKING_STATUS.CANCELLED,
  BOOKING_STATUS.NO_SHOW,
];

const ACTIVE_OPERATING_STATUSES = [
  BOOKING_STATUS.DRIVER_ASSIGNED,
  BOOKING_STATUS.ON_ROUTE,
  BOOKING_STATUS.DRIVER_ARRIVED,
  BOOKING_STATUS.PICKED_UP,
];

const PRE_PICKUP_STATUSES = [
  BOOKING_STATUS.PENDING,
  BOOKING_STATUS.CONFIRMED,
  BOOKING_STATUS.DRIVER_ASSIGNED,
  BOOKING_STATUS.ON_ROUTE,
  BOOKING_STATUS.DRIVER_ARRIVED,
];

module.exports = {
  ADMIN_BOOKING_VIEWS,
  OPERATIONS_SEVERITY,
  OPERATIONS_THRESHOLDS,
  DEFAULT_HISTORY_DAYS,
  TERMINAL_BOOKING_STATUSES,
  ACTIVE_OPERATING_STATUSES,
  PRE_PICKUP_STATUSES,
};
